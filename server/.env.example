# Copy to .env and adjust if needed. Defaults work with a local MongoDB.
MONGO_URI=mongodb://127.0.0.1:27017/ticket-sys
PORT=4000
# Signing key for auth tokens. A dev fallback exists so local setup needs no
# config, but ANY real deployment must set a long random value here.
JWT_SECRET=

