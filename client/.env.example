# Netlify → Site settings → Environment variables
# Your deployed API origin (no trailing slash). Netlify proxies /api/* here so
# the browser stays same-origin and the backend needs no CORS changes.
# Example: https://ticket-api.onrender.com
API_PROXY_TARGET=
